spring:
  datasource:
    url: jdbc:mysql://localhost:3306/garbage_classifier?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf-8&allowPublicKeyRetrieval=true
    username: root
    password: qq18813717623
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# 百度AI配置（你的应用信息）
baidu:
  api:
    # 你的 AppID: 7810199
    app-id: 7810199
    # 你的 API Key
    api-key: S4jEfpnTjtrE0ofYtvP1pp1g
    # 你的 Secret Key
    secret-key: lgSEyTRKRXufHyydraL3vQmpJ460gqJv
    # 垃圾分类API地址
    url: https://aip.baidubce.com/rest/2.0/image-classify/v1/garbage

server:
  port: 8080

logging:
  level:
    com.example: DEBUG
    org.springframework.web: INFO

jwt:
  secret: mySecretKeyForGarbageClassifierProject2026  # 密钥，可以随便改成复杂的字符串
  expiration: 86400  # 有效期，单位是秒（86400 = 24小时）